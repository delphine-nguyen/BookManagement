{% extends 'base.html.twig' %}

{% block title %}New Editor
{% endblock %}

{% block main %}
	<h1>Create new Editor</h1>
	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="btn">{{ button_label|default('Save') }}</button>
	{{ form_end(form) }}


	<a href="{{ path('app_editor_index') }}">back to list</a>
{% endblock %}
